import React from 'react';
import ReactDOM from 'react-dom/client';
import DevShell from './dev/DevShell.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DevShell />
  </React.StrictMode>
);
